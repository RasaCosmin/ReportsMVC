@model MVCReports.Models.AccuracyViewModel

<body>
    <div class="sidebar-reports">
        <p>
            <label for="customer-dropdown-list" >Select Email: </label>
            @Html.DropDownListFor(m => Model.Email, new SelectList(Model.Customers.Select(c => c.Name).ToList(), Model.Customers.First()), new { id = "customer-dropdown-list" })
        </p>
    </div>
    <input value="Generate Report" type="submit" class="btn btn-default submit mt20 " id="generate-report-btn" />
</body>
